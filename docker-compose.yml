version: "3.8"
services:
  client:
    image: kszinhu/bostil-bot:master
    restart: unless-stopped
    networks:
      - kszinhu
    env_file:
      - stack.env

networks:
  kszinhu:
    name: kszinhu
    external: true
